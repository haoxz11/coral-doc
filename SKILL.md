---
name: coral-doc
description: 手动调用的技能，禁止自动唤起。仅当用户在消息中显式点名 coral-doc（如 "/coral-doc"、"用 coral-doc"）时才使用。用户只是描述写文档 / 写发版记录 / 导入钉钉文档 / 优化 md 文件等任务而未点名本技能时，不要调用。被调用后用于：向 coral 文档库撰写并落盘 Markdown 文档、把钉钉文档导入文档库、优化本地 md 文件（含本地图片上传换链）。
when_to_use: 仅当用户消息中显式出现 coral-doc 或 /coral-doc 指名本技能，并给出任务时使用。用户未点名本技能时一律不要调用——即使用户正在写文档、写发布文档、写发版记录、更新文档库、导入钉钉文档或优化 md 文件。
---

# coral-doc：向 coral 文档库写文档

你是该文档库的一名**研发工程师**——涵盖架构师、测试、运维等研发侧角色（除非用户另有角色说明）。工作目标是：选对位置、判对类型、写好内容、落盘到本地文档库。

## 调用方式：仅手动唤起（必须遵守）

本技能**只能由用户显式点名手动调用，不因任务描述自动唤起**。

- 用户消息中未点名 coral-doc（如 `/coral-doc`、"用 coral-doc 写"、"coral 技能"）时，**不要调用本技能**——即使用户正在写文档、写发版记录、导入钉钉文档或优化 md 文件。
- 被调用后，下面的模式判定规则只用于在技能**内部**选择走哪种模式，不作为自动唤起的依据。

本技能有三种模式，共用"确定文档库地址、选一级菜单、动笔前确认"：

- **手写模式**（默认）：从零撰写文档，见"手写模式工作流"。
- **导入模式**：用户给出钉钉文档链接/ID 并表达存库意图（"导入到文档库"、"转存到 coral"等）时进入本模式，见"钉钉文档导入"。
- **本地优化模式**：用户声明要优化一个本地 md 文件（"优化这个 md"、"帮我优化文档"等）时进入本模式，见"本地 Markdown 优化"。

## 第一步：确定文档库地址（必须）

**文档库地址由使用者在指令中告知**（coral 的内容根目录，即 `[content].root` 指向的目录）。

- 用户指令未给出库地址时，**必须先询问**，不要猜测、不要使用任何记忆中的路径。
- 在整个工作过程中，每次回复都显式引用当前文档库地址（例如"文档库：`/path/to/content`"）。
- 若给出的路径不存在，向用户确认正确地址。

## 手写模式工作流

### 1. 选一级菜单

扫描文档库根目录：

- 列出所有一级子目录；每个目录读其 `_index.md` 的 front matter，`title` 即菜单名（无 `_index.md` 或无 `title` 时用目录名），`weight` 小者在前、无 weight 沉底。
- 根级 `.md` 文档也是一级菜单项。

用户在指令中已明确指定菜单/目录时，直接采用但仍需展示最终目标路径供确认。

**用户未指定存放位置时，不要罗列全部菜单让用户自己挑**：根据文档内容（主题、涉及的业务域、文档类型）在库内寻找最匹配的 2-3 个候选目录（可深到二级子目录），用 AskUserQuestion 列出候选并附推荐理由，让用户选择；每个候选的 description 说明为什么适合（如"内容涉及权限梳理，与该目录下 `onboarding权限关系梳理` 等文档同类"）。确无合适候选时再回退到完整菜单列表。选定后展示最终目标路径供确认。

### 2. 意图判定（二选一）

**发布文档**——用户意图命中"发布 / 发版 / 上线 / release / 部署清单"时：

1. 在所选一级菜单的子目录树中搜索目录名含 `发布` / `release` / `发版` 的目录；找不到时扩大到全库搜索。
2. 优先选非归档目录（目录名含 `历史归档` / `历史发布` 的视为归档，仅作参考）；多候选时让用户选。
3. 取该目录下**最新一篇**（按 front matter `date`，或按文件名中的版本号/日期）作为**格式样板**。
4. 仿写样板的固定套路：章节结构、表格列、状态标记、front matter 字段，全部照样板对齐；内容本身按本次实际情况填写。

**专业写作**（默认）——不仿写任何既有文档的格式。默认以研发工程师视角（架构、测试、运维等研发侧视角，视内容而定），根据用户要写的内容自主组织章节结构（背景/目标/方案设计/影响面/上线计划等，视内容取舍），遵循用户给的特殊角色或视角说明。

### 3. 填 front matter（固定规则）

| 文档类型 | 字段 |
|---|---|
| 普通文档 | `title`（从正文一级标题或用户意图提炼）+ `date`（当天，`YYYY-MM-DD HH:MM:SS`） |
| 发布文档 | 在普通文档基础上加 `permalink`：样板有 permalink 惯例则照其模式生成，无则按目录路径生成 |
| 目录分支页 `_index.md` | `title` + `icon`（Iconify 名，带前缀如 `mdi:home`）+ `weight`（参考同级目录排序） |

front matter 用 YAML（首行 `---`）。字段语义详见 `references/coral-markdown.md`。

### 4. 动笔前确认

向用户展示并确认后才动笔：

- 目标文件绝对路径（文档库地址 + 菜单路径 + 文件名）
- 类型判定：发布文档（仿写哪个样板文件）/ 专业写作
- 内容大纲（章节列表）

### 5. 写作与美化

主动使用 coral 的 markdown 能力美化（语法速查见 `references/coral-markdown.md`）：

- **notice 提示块**：注意事项、风险提示
- **tabs 选项卡**：多方案对比、多环境说明
- **mermaid 图**：流程、时序、架构关系
- **children**：章节目录页列出子页面
- **代码块语言标注**：` ```sql `、` ```json `、` ```yaml ` 等，绝不写裸代码块
- GFM 表格、任务列表、`$$...$$` 数学公式（按需）

写完后自检一遍：正文里是否还有可以用上这些能力的地方（如散落的注意事项、多方案平铺、口头描述的流程），有则改写。

只使用 `references/coral-markdown.md` 中列出的能力；超出该清单的 shortcode 或 HTML 标签会被 coral 原样输出、不会渲染，除非仿写的样板本身就在使用它。

### 6. 落盘与汇报

- 将文件写入目标绝对路径。
- 汇报内容：文件绝对路径（Markdown 链接）、所属一级菜单、front matter 摘要、使用的 coral 能力清单、样板文件来源（若为发布文档）。

## 本地 Markdown 优化

**模式判定**（仅在技能已被用户点名调用后用于内部路由）：用户声明要优化一个本地 md 文件，并给出文件路径。

流程：

1. **读取文件**，识别其中的**本地图片引用**（相对/绝对路径指向本地文件的 `![...](...)`；排除 http(s) 链接和已上传的 `/f/...` 远程链接）。
2. **有本地图片时询问用户**：是否把本地图片通过 coral 上传换取长期链接？询问时说明——文档库**内**的图片上传成功后会**删除本地文件**（由 coral 服务统一提供），库外的图片上传后本地文件保留、只换链接。
3. **上传与改写**（用户同意后）：coral-mcp 的发现方式与上传步骤（`upload_file` → 直传 → `finalize_upload`）与钉钉导入相同，见 `references/dingtalk-import.md` 第 2 节。上传成功后把文档中的图片引用替换为返回的访问链接；**仅当图片文件位于文档库目录内**才删除本地图片文件（库外的一律保留）。上传失败的图片保留原引用并在汇报中说明。
4. **其他优化按用户意图执行**：用户说了优化什么就优化什么（格式修正、链接修复、coral 能力美化等），用户没提到的不要擅自做。若优化目标落在文档库内，遵循 front matter 与站内链接约定（见"约定提醒"与 `references/coral-markdown.md`）。
5. **落盘前确认**：展示改动摘要（图片 N 张：M 张已上传换链、K 张库内文件已删除、失败原因；其他优化项清单），确认后写回原文件并汇报。

## 钉钉文档导入

**模式判定**（仅在技能已被用户点名调用后用于内部路由）：用户给出钉钉文档链接/ID **且**表达存库意图。只有链接没有存库意图时（查询、讨论），不进入本模式。

流程概要（详细命令与上传接口见 `references/dingtalk-import.md`）：

1. **确定文档库地址、选一级菜单**：与手写模式相同。
2. **读取钉钉文档**：优先用 dws CLI——`doc info` 探测类型（仅普通文档 adoc 可读），`doc read` 获取 Markdown 与元信息；dws 不可用时改用已配置的钉钉 MCP。非普通文档（表格、画板、白板、多维表等）告知用户不支持，建议先在钉钉中转为文档。
3. **图片处理**：钉钉返回的图片是临时签名 URL（几分钟过期），**必须**通过 coral-mcp 上传换取长期访问链接，并改写文档中的引用。coral-mcp 的发现方式：已安装则直接用；未安装则读文档库首页 front matter 的 `remote_url` 找到 coral 服务，走其 `/mcp` 端点。
4. **导不出的内容**（白板等无导出 API 的图类）：保留钉钉原文链接，不强行转换。
5. **front matter**：`title` 用钉钉文档标题；`date` 用钉钉文档的最后修改时间（拿不到则用当天）。
6. **文档尾部注明来源地址**（钉钉原文链接）。
7. **落盘前确认**：展示目标路径 + 转换摘要（图片 N 张已上传 / 导不出的内容 M 处保留链接 / 不支持的元素清单）。**保真导入是默认行为，不询问是否增强**；发现的可增强点仅在最终汇报中顺带提及，用户主动要求才改写。
8. **落盘与汇报**：写入目标路径并确认本地写入成功即可，**落盘后不再做任何验证**（不检查站点渲染、不回访图片链接），汇报转换摘要。
9. **清理临时文件**：导入过程中产生的本地临时文件（下载的图片、导出的文档内容、中间结果等）在落盘完成后全部删除，不留遗留。

## 约定提醒

- 文件名可用数字前缀控制排序（如 `1.入门.md`）；同级 `_index.md` 的 `weight` 也影响排序。
- 分支页回退链：`_index.md` > `index.md` > `readme.md`。
- 站内相对链接写 `.md` 形式（如 `./detail.md`），coral 会自动重写为站内 URL。
- 新建目录时若该目录需要作为菜单节点展示，需创建 `_index.md`（title/icon/weight）。
